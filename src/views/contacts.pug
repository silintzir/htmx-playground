table
  thead
    tr
      th First name
      th Last name
      th Phone
      th Email
  tbody
    each contact in #{contacts}
      tr
        td {contact.firstName}
        td {contact.lastName}
        td {contact.phone}
        td {contact.email}

